#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 *@argc: number of arguments typed
 *@argv: its the array pointing to the arguments
 *Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
